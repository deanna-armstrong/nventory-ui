// src/environments/environment.ts

export const environment = {
  // When false, Angular runs in development mode:
  // - enables runtime checks
  // - preserves debug information
  production: false,

  // Base URL for API calls during local development.
  // Points at your locally running NestJS server.
  apiUrl: 'http://localhost:3000/api',
};
