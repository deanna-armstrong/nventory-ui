// src/environments/environment.prod.ts

export const environment = {
  // Flag indicating this build is for production;
  // Angular disables dev-mode assertions and logging when true.
  production: true,

  // Base URL for API calls in the production environment.
  // Points at the deployed nventory-api instance.
  apiUrl: 'https://nventory-api.onrender.com/api'
};
